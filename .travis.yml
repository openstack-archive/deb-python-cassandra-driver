language: python
python:
  - "2.7"
  - "3.4"
  - "3.6"
  - "pypy"

sudo: false
env:
  - CASS_DRIVER_NO_CYTHON=1

addons:
  apt:
    packages:
    - build-essential
    - python-dev
    - pypy-dev
    - libev4
    - libev-dev

install:
  - pip install tox

script:
  - tox
